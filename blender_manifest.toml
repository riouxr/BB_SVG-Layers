schema_version = "1.0.0"

id = "svg_layer"
version = "2.2.0"
name = "SVG Layer"
tagline = "Convert SVG layers: rotate, scale, merge, offset, UV-project and solidify"
maintainer = "Blender Bob & Claude"
type = "add-on"

blender_version_min = "4.2.0"

license = ["SPDX:GPL-2.0-or-later"]

[permissions]
# No special permissions needed
